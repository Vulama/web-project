<!DOCTYPE html>
<html>

<head>
    <title>Utakmica</title>
    <meta http-equiv="content-type" content="text/html; charset=utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body class="content">

<main class="main">
    <div>
        <% if (user) { %>
            <h1><a href="/logout">Logout</a></h2>
        <% } else { %>
            <h1><a href="/login">Login</a></h2>
        <% } %>
    </div>

    <div>
        <h1><a href="/">Go Back</a></h2>
    </div>
    
    <div>
        <% if (user) { %>
            <h2><%= user.name %></h2>
            <h2><%= user.nickname %></h2>
        <% } else { %>
            <h2>Unknown</h2>
        <% } %>
        <hr></hr>
        <h2 style="text-align: center"><%= utakmica.team1 + " " + utakmica.gol1 + " - " + utakmica.gol2 + " " + utakmica.team2 %> </h2>
        <% for(var comm of utakmica.comments) { %>
            <% if(utakmica.comments.indexOf(comm) % 2 == 0){ %>
                <div style="background-color: aliceblue" class="utakmica">
            <% } else { %>
                <div style="background-color: beige" class="utakmica">
            <% } %>
            <div class="column">
                <p><%= comm.owner %> </p>
                <p><%= comm.date %> </p>
            </div>
            <a href=<%='/utakmica/'+ utakmica.id + "?del=" +  comm.id %>   style="float: right; margin-right: 5%; margin-top: 1%;">Delete</a>
            <h2 style="text-align: center; margin-right: 10%; margin-left: 10%;"><%= comm.text %> </h2>
            
            </div>
        <% } %>
        <hr></hr>
        <form style="text-align: center">
            <label for="comm">Comment</label><br>
            <input type="text" id="comm" name="comm"><br>
            <input type="submit" value="Submit">
        </form>
    </div>

</main>

</body>

</html>