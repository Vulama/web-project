<!DOCTYPE html>
<html>

<head>
    <title>Web</title>
    <meta http-equiv="content-type" content="text/html; charset=utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" href="./styles/main.css">
</head>

<body class="content">

<main class="main">
    <div>
        <% if (user) { %>
            <h1><a href="/logout">Logout</a></h2>
        <% } else { %>
            <h1><a href="/login">Login</a></h2>
        <% } %>
    </div>
    
    <div>
        <% if (user) { %>
            <h2><%= user.name %></h2>
            <h2><%= user.nickname %></h2>
            <h2><%= data.utakmice[0].team1 %></h2>
        <% } else { %>
            <h2>Unknown</h2>
        <% } %>
    </div>

</main>

</body>

</html>
